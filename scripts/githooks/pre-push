#!/usr/bin/env bash
# Format/lint hooks run on commit only (scripts/githooks/pre-commit → .pre-commit-config.yaml).
# Pre-push intentionally does not re-run pre-commit: push would fail after mutating files
# (e.g. end-of-file-fixer) that were already fixed at commit time.
#
# Enable commit hooks for this clone: git config core.hooksPath scripts/githooks
exit 0
