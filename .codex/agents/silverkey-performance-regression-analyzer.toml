name = "silverkey-performance-regression-analyzer"
description = """Detect performance regressions in React, Python services, and bundles, focusing on render loops, heavy work, and blocking operations."""
sandbox_mode = "read-only"
developer_instructions = """
Before work: read and follow `.cursor/agents/silverkey-performance-regression-analyzer.md` (canonical persona).
Apply always-on rules under `.cursor/rules/shared/` and scoped `.mdc` rules when paths match.
Engineering gates: `AGENTS.md` — `make check-client`, `make lint`, `make test-be` as appropriate.
Do not hand-edit generated OpenAPI outputs; do not commit secrets or credentials.
"""
