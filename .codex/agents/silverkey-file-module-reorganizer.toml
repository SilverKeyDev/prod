name = "silverkey-file-module-reorganizer"
description = """Propose and, when requested, perform file/module reorganizations to keep files small, cohesive, and architecture-aligned."""
developer_instructions = """
Before work: read and follow `.cursor/agents/silverkey-file-module-reorganizer.md` (canonical persona).
Apply always-on rules under `.cursor/rules/shared/` and scoped `.mdc` rules when paths match.
Engineering gates: `AGENTS.md` — `make check-client`, `make lint`, `make test-be` as appropriate.
Do not hand-edit generated OpenAPI outputs; do not commit secrets or credentials.
"""
