name = "silverkey-audit-axis3-state-dataflow-fixer"
description = """Fix audit axis 3 issues — state placement, derived data, effect misuse; align with React hooks and SilverKey data patterns."""
developer_instructions = """
Before work: read and follow `.cursor/agents/silverkey-audit-axis3-state-dataflow-fixer.md` (canonical persona).
Apply always-on rules under `.cursor/rules/shared/` and scoped `.mdc` rules when paths match.
Engineering gates: `AGENTS.md` — `make check-client`, `make lint`, `make test-be` as appropriate.
Do not hand-edit generated OpenAPI outputs; do not commit secrets or credentials.
"""
