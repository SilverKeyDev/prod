# Use hoisted node_modules so Metro (Expo/React Native) can resolve
# packages like @babel/runtime. pnpm's default isolated layout uses
# symlinks that Metro's resolver doesn't follow for web bundling.
# See: https://docs.expo.dev/guides/monorepos/#package-managers-with-isolated-dependencies
node-linker=hoisted
