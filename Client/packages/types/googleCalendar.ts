/**
 * Google Calendar API surface types for packages outside the calendar feature.
 * Re-exports from the calendar module so consumers avoid cross-feature api imports.
 */

export type { GoogleCalendar, GoogleEvent } from "packages/features/calendar/api/types";
