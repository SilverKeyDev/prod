/**
 * Single source of truth for saved page UI constants.
 */

/**
 * Shared input className for the saved page search field (tabs layout and main layout).
 * Web: use with `Input` `leftIcon` so the control gets `has-left-icon` left padding.
 * Native: add leading padding (e.g. `pl-10`) when rendering a leading search icon beside `TextInput`.
 */
export const SAVED_PAGE_SEARCH_INPUT_CLASS = "mobile-input h-11 w-full pr-4 font-sans text-sm";
