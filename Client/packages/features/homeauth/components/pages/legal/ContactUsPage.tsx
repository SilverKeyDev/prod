import { ContactUsFeature } from "@/features/homeauth/components/legal/ContactUsFeature";

/**
 * Web Contact Us page. Uses shared ContactUsFeature (and ContactUsContent) so
 * copy and structure match native ContactUsScreenNative.
 */
export default function ContactUsPage() {
  return <ContactUsFeature />;
}
