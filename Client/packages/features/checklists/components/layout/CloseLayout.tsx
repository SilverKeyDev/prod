/**
 * CloseLayout re-exports ChecklistLayout, which includes ChecklistIntegrationSlot
 * for rendering integration components (e.g. HomeConcierge/MoveConcierge) when
 * the active checklist item has a component_key. Used by ClosingMovingIn and
 * other close-page section subheaders.
 */
export { default } from "./ChecklistLayout";
