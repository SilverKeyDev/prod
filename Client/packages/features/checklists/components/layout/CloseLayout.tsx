/**
 * CloseLayout re-exports ChecklistLayout, which includes ChecklistIntegrationSlot
 * for rendering integration components (e.g. partner transaction placement) when
 * the active checklist item has a component_key.
 */
export { default } from "./ChecklistLayout";
