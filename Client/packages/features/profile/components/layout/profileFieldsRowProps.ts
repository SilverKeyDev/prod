/** Default props for paired profile/settings fields (container-aware via AlignedRow). */
export const PROFILE_FIELDS_ROW_PROPS = {
  breakIntoRows: "sm" as const,
  gap: "lg" as const,
  justify: "start" as const,
};
