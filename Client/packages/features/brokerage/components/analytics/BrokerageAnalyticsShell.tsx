import { Box } from "packages/ui/components/structure/primitives";

/** Empty analytics shell for brokerage workspace — content ships in a follow-up. */
export function BrokerageAnalyticsShell() {
  return <Box />;
}
