// Backwards-compat shim: re-export provider-agnostic agreement types from agreements.ts
// so existing imports continue to work while we migrate away from DocuSign-specific naming.
export * from "./agreements";
