/**
 * Shape passed to NegotiationModal as `initialHome` (matches Saved `convertToFavoriteHome` output).
 */
export type NegotiationInitialHome = {
  user_id: string;
  address: string;
  beds: string;
  baths: string;
  sqft: string;
  lot_size: string;
  price: string;
  image_url: string;
  created_at: string;
  updated_at: string;
};
