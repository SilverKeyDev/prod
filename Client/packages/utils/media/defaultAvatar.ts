/**
 * Public path to the default profile silhouette (matches `public/default-avatar.svg` on web).
 * Native uses a bundled PNG from `logoSource.native.ts` for the same artwork.
 */
export const DEFAULT_AVATAR_WEB_PATH = "/default-avatar.svg";
