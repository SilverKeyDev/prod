/**
 * React Native Maps `latitudeDelta` / `longitudeDelta` for property-detail maps
 * (~parcel scale; aligned with neighborhood zoom used on web).
 */
export const PROPERTY_DETAILS_MAP_REGION_DELTA = 0.00135;
