/**
 * Fixed height for header bar rows (44px). Use for search header and any header bar
 * so all controls (buttons, triggers, chips) align to the same height.
 */
export const HEADER_ROW_HEIGHT = "h-11 min-h-11 max-h-11";
