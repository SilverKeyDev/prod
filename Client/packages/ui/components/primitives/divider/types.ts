export type DashedDividerProps = {
  /** Additional className */
  className?: string;
  /** Orientation: horizontal (default) or vertical */
  orientation?: "horizontal" | "vertical";
  /** Line color (native only; web uses Tailwind) */
  color?: string;
};
