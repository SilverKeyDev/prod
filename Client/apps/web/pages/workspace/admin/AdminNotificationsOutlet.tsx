import { AdminPlaceholderSection } from "packages/features/admin";

export default function AdminNotificationsOutlet() {
  return (
    <AdminPlaceholderSection
      title="Notifications"
      description="System-wide broadcasts, SLA alerts, provider webhooks health, and product announcements will plug in here."
    />
  );
}
