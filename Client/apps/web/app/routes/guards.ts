/**
 * Authentication guards for routing
 */

import { checkAuthAndRedirect } from "../../../../packages/utils/auth";

// Re-export the checkAuthAndRedirect function for routing guards
export { checkAuthAndRedirect };
