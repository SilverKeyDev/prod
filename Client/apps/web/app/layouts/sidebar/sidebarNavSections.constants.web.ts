/** Primary nav labels: inactive at sm; active one step up (base) so the gap is smaller than former xs→sm + icon jump. */
export const sidebarNavLabelInactive = "text-sm font-medium";
export const sidebarNavLabelActive = "text-base font-bold leading-snug";

export const SINGLE_LINK_KEYS = new Set(["dashboard", "search", "decide", "profile", "agent"]);
