name: Test & Coverage

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  tests:
    uses: ./.github/workflows/test-callable.yml
