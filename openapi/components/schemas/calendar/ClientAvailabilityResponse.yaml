allOf:
  - $ref: '../shared/core/SuccessResponse.yaml'
  - type: object
    properties:
      availability:
        type: array
        items:
          type: object
          properties:
            start:
              type: string
              format: date-time
            end:
              type: string
              format: date-time
            status:
              type: string
              enum: [free, busy]
