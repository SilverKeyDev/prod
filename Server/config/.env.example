# Auto-generated by scripts/load-all-secrets.sh on 2026-01-17T22:32:05Z
# Region: us-east-2

# From secret: db_url (json)
DATABASE_URL=

# From secret: AWS_Access (json)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# From secret: cognito (json)
AWS_COGNITO_CLIENT_ID=
AWS_COGNITO_CLIENT_SECRET=
AWS_COGNITO_USER_POOL_ID=

# From secret: gmaps (json)
GOOGLE_MAPS_API_KEY=

# From secret: google_calendar (json)
GOOGLE_CALENDAR_SECRET=
GOOGLE_CLIENT_ID=
# Optional. If unset, redirect URI is derived from FLASK_ENV. Must match an Authorized redirect URI
# on the same OAuth 2.0 Web client as GOOGLE_CLIENT_ID (Google Cloud Console → Credentials).
# GOOGLE_REDIRECT_URI=http://127.0.0.1:5000/api/v1/google/oauth/callback
#
# Register these exact URIs on that client (calendar connect vs Google Sign-in use different paths):
#   http://localhost:5000/api/v1/google/oauth/callback
#   http://localhost:5000/api/v1/auth/google/callback
# Production:
#   https://usesilverkey.com/api/v1/google/oauth/callback
#   https://usesilverkey.com/api/v1/auth/google/callback
# If you set GOOGLE_REDIRECT_URI, add that exact string to Authorized redirect URIs as well.

# From secret: census_api (json)
CENSUS_API_KEY=

# From secret: mapbox (json)
MAPBOX_API_KEY=

# From secret: openai (json)
OPENAI_KEY=

# From secret: perplexity (json)
PERPLEXITY_API_KEY=

# From secret: plaid (json)
PLAID_SANDBOX_KEY=
PLAID_SECRET=
VITE_PLAID_CLIENT_ID=

# From secret: serp (json)
SERP_API=

# From secret: rapidapi (json)
RAPIDAPI_KEY=

# From secret: skyslope (json)
SKYSLOPE_ACCESS_KEY=
SKYSLOPE_SECRET=
# Optional. If unset, redirect URI is derived from FLASK_ENV. Must match the callback URL
# SkySlope registered for this OAuth client (they configure it—no self-service portal).
# SKYSLOPE_REDIRECT_URI=https://your-domain/api/v1/skyslope/callback
