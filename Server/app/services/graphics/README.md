# Graphics service

Shared matplotlib chart generators live in `charts/matplotlib_charts.py`. Import via `from app.services.graphics import generate_horizontal_bar_chart, ...`.

Maps and research-specific PDF layout remain under `app/services/research/graphs/`.
