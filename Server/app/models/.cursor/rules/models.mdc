---
description: Models rules (schemas, ORM, migrations compatibility)
globs:
  - "Server/app/models/**"
alwaysApply: false
---
# Models
- Keep models cohesive and documented; defaults and constraints explicit.
- Migrations must reflect model changes; avoid destructive changes without migration scripts.
- Handle PII carefully; encrypt/hash sensitive fields where applicable.
