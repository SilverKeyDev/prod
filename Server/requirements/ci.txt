# =============================================================================
# requirements/ci.txt — CI LINT / IMPORT SMOKE (SLIM)
# =============================================================================
#
# What this is:
#   Enough packages for scripts that must import the `app` package (e.g. Ruff against
#   Server/, lint_circular_imports.py: `from app import create_app`) and for Pyright to
#   resolve optional ML/geo imports under app/services/{research,search/**} without
#   installing torch (torch + sentence-transformers stay in requirements/test.txt).
#
# Where it is used:
#   - Layered by requirements/lint.txt (CI lint) and requirements/test.txt (CI pytest)
#   - Backend tests job (first layer): .github/workflows/test-callable.yml installs ci.txt,
#     then CPU-only torch, then requirements/test.txt
#   - Local slim venv: Server/scripts/bootstrap-venv.sh --ci (import smoke) or --lint (CI lint parity)
#
# Relationship to other files:
#   - Production / Docker → requirements/runtime.txt (superset of this file)
#   - Pytest on CI needs torch + sentence-transformers → also install requirements/test.txt
#     (and CPU torch per test-callable.yml), not this file alone
#   - When something is imported from app/__init__.py or modules loaded at import time,
#     add it here AND in runtime.txt; keep overlapping pins aligned with runtime where they
#     must match (e.g. flask-compress is duplicated in test.txt for the test install path)
#
# --- Core Flask & Extensions ---
Flask==3.1.3
Werkzeug==3.1.6
flask_sqlalchemy==3.0.5
flask_login==0.6.3
flask_cors==6.0.0
flask-compress==1.15
flask_wtf==1.3.0
WTForms==3.2.1
flask_marshmallow==0.15.0
flask_migrate==4.0.5
alembic==1.16.5
Mako==1.3.12
flask_executor==1.0.0
flask_jwt_extended==4.7.1

# --- PDF, API, and Web ---
pypdf==6.11.0
reportlab==4.0.4
stripe==9.4.0
requests==2.34.2
urllib3==2.7.0
python-dotenv==1.2.2
gunicorn==26.0.0
boto3==1.34.162
botocore==1.34.162
s3transfer==0.10.4
jmespath==1.0.1
PyJWT==2.12.0
python-jose[cryptography]==3.4.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.43
pyOpenSSL==26.2.0
usaddress==0.5.10
pillow==12.2.0

# --- Security Dependencies ---
# python-magic wraps system libmagic: macOS `brew install libmagic`, Debian `libmagic1`
python-magic==0.4.27

# --- DocuSign Dependencies ---
docusign-esign==3.25.0
pycryptodome==3.20.0

# --- Google Calendar OAuth Dependencies ---
google-auth>=2.26.1,<3.0dev
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0
google-api-core==2.25.1
googleapis-common-protos==1.70.0
proto-plus==1.26.1
grpcio==1.75.0
httplib2==0.31.0
cryptography==48.0.0

# --- Pydantic & Serialization ---
pydantic==2.10.5
email-validator==2.2.0
marshmallow==3.26.2
marshmallow-sqlalchemy==1.5.0

# --- Background Tasks ---
celery==5.3.4
redis==7.4.0

plaid-python==36.1.0
tenacity==8.2.3

# --- Analytics / observability (app.posthog_client imports posthog at module load; pins aligned with runtime.txt) ---
posthog>=3.0.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# --- HTTP / LLM clients (keep pins aligned with runtime.txt) ---
openai==1.76.2
httpx>=0.24.1,<1

# --- Data / ML / Geo (Pyright + import smoke; pins aligned with runtime.txt / test.txt) ---
numpy>=1.26.0,<3
pandas>=2.0.0,<3
matplotlib>=3.8.0,<4
scikit-learn==1.5.0
shapely>=2.0.6,<2.1
