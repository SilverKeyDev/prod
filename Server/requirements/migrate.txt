# =============================================================================
# requirements/migrate.txt — ALEMBIC / flask db upgrade ONLY
# =============================================================================
#
# Used by Dockerfile.web target `migrate` and .github/scripts/gh-db-upgrade.sh.
# Only packages needed to boot Flask + SQLAlchemy + run migrations (not the API).
#
# --- Core Flask & extensions (imported from app/__init__.py) ---
Flask==2.3.3
Werkzeug==2.3.7
flask_sqlalchemy==3.0.5
flask_login==0.6.2
flask_cors==4.0.0
flask-compress==1.15
flask_wtf==1.1.1
WTForms==3.2.1
flask_marshmallow==0.15.0
flask_migrate==4.0.5
alembic==1.16.5
Mako==1.3.12
flask_executor==1.0.0
flask_jwt_extended==4.5.2

# --- Database ---
python-dotenv==1.2.2
psycopg2-binary==2.9.9
SQLAlchemy==2.0.43

# --- Serialization (models / marshmallow) ---
marshmallow==3.20.2
marshmallow-sqlalchemy==0.28.1
pydantic==2.10.5
email-validator==2.2.0
